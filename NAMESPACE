
exportMethods("addLevels",
              "removePrefix",
              "validateChr",
              "addPrefix",
              "sortChr",
              "orderChr",
              "replaceChr",
              "setPar",
              "getPar")

 exportClasses("numericOrNULL",
              "characterOrNULL",
              "vectorOrNULL",
              "GraphicPars",
              "VisnabView",
              "CircularView",
              "IntervalView",
              "AlignmentView",
              "SeqView",
              "StackedView")


## for scales
##importFrom("scales", alpha, cscale, dscale)
## importFrom(plyr, is.discrete)
## importFrom(RColorBrewer, brewer.pal)
importClassesFrom(BSgenome,BSgenome)

importClassesFrom(IRanges, RangedData, RangedDataList, DataTable,
                  Sequence, DataTableORNULL, characterORNULL,
                  RangedSelection, SimpleList)

importFrom(IRanges, DataFrame, IRanges, RangedDataList, RangedData,
           RangesList, Rle, RangedSelection)

importMethodsFrom(IRanges, elementLengths, window,"[",
                  start, end, width, range,
                  "start<-", "end<-", "width<-",
                  split, lapply, unlist, unique,
                  as.vector, as.list, as.data.frame,
                  rbind, cbind, colnames,  rownames,
                  "colnames<-","rownames<-",
                  ranges, values, score,
                  "ranges<-", "values<-", "score<-",
                  space, universe, metadata,
                  "universe<-", "metadata<-",
                  isDisjoint, queryHits, findOverlaps, reduce,
                  disjointBins, elementMetadata, show)

importClassesFrom(GenomicRanges, GRanges, GRangesList)

importFrom(GenomicRanges, GRanges, GRangesList)

## importFrom(GenomicFeatures, getChromInfoFromUCSC)

importMethodsFrom(GenomicRanges, seqnames, seqlengths, strand, "strand<-",
                  elementMetadata)

importClassesFrom(rtracklayer, BrowserView)

importFrom(rtracklayer, browserSession, genome, "genome<-",
           ucscTableQuery, tableNames, getTable, GenomicData,
           "track<-", GRangesForUCSCGenome, browserView)

## importFrom(plyr, is.discrete)
## importFrom(RColorBrewer, brewer.pal)

## Rsamtools
importFrom(Rsamtools, ScanBamParam, scanBam)

export(ScanBamParam, scanBam, IRanges, RangesList,
       DataFrame, IRanges, RangedDataList, RangedData,
       RangesList, Rle, RangedSelection,
       elementLengths, window,
       start, end, width, range,
       "start<-", "end<-", "width<-",
       split, lapply, unlist, unique,
       as.vector, as.list, as.data.frame,
       rbind, cbind, colnames,  rownames,
       "colnames<-","rownames<-",
       ranges, values, score,
       "ranges<-", "values<-", "score<-",
       space, universe, metadata,
       "universe<-", "metadata<-",
       isDisjoint, queryHits, findOverlaps, reduce,
       disjointBins, elementMetadata,
       "[", show,
       GRanges,seqnames)

exportPattern("^[^\\.]")
